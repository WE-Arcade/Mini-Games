// TrainIcon.js
import React from "react";
// import "./TrainIcon.css"; // Make sure to import the CSS file

const TrainIcon = () => {
  return (
    <img
      src="icons/train.ico" // Replace this with your train icon path
      alt="Moving Train"
      className="train-icon"
    />
  );
};

export default TrainIcon;
